// JSON.parse方法用来解析JSON字符串，构造由JacaScript值或对象。
// 该方法有两个参数：第一个参数是需要解析处理的JSON字符串，第二个参数是可选参数提供可选的 reviver 函数，用在返回之前对所得到的对象执行变换操作。
// 该方法的语法为：JSON.parse(text[, reviver])

// 数组改变自身的方法;pop、push、reverse、shift、sort、splice、unshift,以及copyWithin、fill


// concat、join、slice、toString、toLocaleString、indexOf、lastIndexOf、未形成标准的 toSource，以及 ES7 新增的方法 includes。
